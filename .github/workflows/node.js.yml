name: run

on:
 push:
   branches: ["main"]
   
jobs:
 my_build_job:
  run-on: ubunt-latest
  steps:
   -uses: actions/checkout@v3
   -name: npm install, test and build
     run:|
     npm install
     npm run build
     
  
